# ── Forensic Platform v2.1 — Nginx (HTTP + HTTPS) ─────────────
upstream u_cert    { server cert-portal:3000; }
upstream u_it      { server it-portal:3001; }
# Outils optionnels : résolution DNS dynamique (modes de déploiement partiels)
# HELK / Velociraptor : idem via set $*_upstream

# Port HTTPS externe (docker map FP_HTTPS_PORT:443) — 443 si non précisé
map $http_host $fp_forwarded_port {
  ~:([0-9]+)$ $1;
  default 443;
}

map $http_upgrade $conn_upgrade {
  default upgrade;
  ''      close;
}

# Origines Grafana (Live + CORS) — dynamique selon $host / Origin navigateur
map $http_origin $grafana_cors_origin {
  default $http_origin;
  "" "https://$host";
}
map $http_origin $grafana_proxy_origin {
  default $http_origin;
  "" "https://$host";
}

# MISP CSRF — évite Referer …/misp/misp/… quand l'URI contient déjà /misp/
map $request_uri $misp_referer_fallback {
  ~^/misp  "https://$http_host$request_uri";
  default  "https://$http_host/misp$request_uri";
}
map $http_referer $misp_referer_final {
  ""      $misp_referer_fallback;
  default $http_referer;
}

# ── HTTP : redirect vers HTTPS ─────────────────────────────────
server {
  listen 80 default_server;
  server_name _;

  # Health checks internes sans redirect (pour les healthchecks Docker)
  location /nginx-health { return 200 "OK\n"; }

  include /etc/nginx/generated/ec2-dns-redirect.conf;

  # Tout le reste → HTTPS
  location / {
    return 301 https://$host$request_uri;
  }
}

# ── HTTPS : services complets ──────────────────────────────────
server {
  listen 443 ssl default_server;
  server_name _;
  resolver 127.0.0.11 ipv6=off valid=30s;
  absolute_redirect off;
  port_in_redirect on;

  # Sidecars HELK / Velociraptor — variables + resolver (nginx -t OK même si conteneurs absents)
  set $helk_kibana_upstream helk-kibana:5601;
  set $helk_es_upstream helk-elasticsearch:9200;
  set $velociraptor_upstream velociraptor-server:8000;
  set $vr_bridge_upstream velociraptor-bridge:8097;

  # Outils forensic optionnels — démarrage nginx sans toute la stack
  set $osd_upstream opensearch-dashboards:5601;
  set $grafana_upstream grafana:3000;
  set $opencti_upstream opencti:8080;
  set $thehive_upstream thehive:9000;
  set $ts_upstream timesketch-web:5000;
  set $misp_upstream misp:80;
  set $minio_console_upstream minio:9001;
  set $cortex_upstream cortex:9001;
  set $logstash_upstream logstash:9700;

  # Modèle fp-final2 : CN=forensic-platform, IP publique en SAN (accès https://<IP>/)
  ssl_certificate     /etc/nginx/ssl/forensic.crt;
  ssl_certificate_key /etc/nginx/ssl/forensic.key;
  ssl_protocols       TLSv1.2 TLSv1.3;
  ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305;
  ssl_prefer_server_ciphers on;
  ssl_session_cache   shared:SSL:10m;
  ssl_session_timeout 10m;
  add_header          Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
  add_header          X-Forensic-Platform "Cybercorp-DFIR-Lab" always;
  add_header          X-Content-Type-Options "nosniff" always;
  add_header          Link "</site-info.html>; rel=\"describedby\"" always;

  include /etc/nginx/generated/ec2-dns-redirect.conf;

  # Pages publiques — identification site (crawlers URL filtering / Palo Alto)
  location = /robots.txt {
    alias /etc/nginx/static/robots.txt;
    default_type text/plain;
    access_log off;
  }
  location = /.well-known/security.txt {
    alias /etc/nginx/static/.well-known/security.txt;
    default_type text/plain;
    access_log off;
  }
  location = /site-info.html {
    alias /etc/nginx/static/site-info.html;
    default_type text/html;
    access_log off;
  }

  client_max_body_size    5G;
  proxy_read_timeout      3600s;
  proxy_send_timeout      3600s;
  proxy_connect_timeout   120s;
  proxy_http_version      1.1;
  proxy_buffering         off;
  proxy_request_buffering off;

  proxy_set_header Host               $host;
  proxy_set_header X-Real-IP          $remote_addr;
  proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto  https;

  location /nginx-health { return 200 "OK\n"; add_header Content-Type text/plain; }

  # ── Portail IT /it/ (upload chiffré TLS) ──────────────────
  location /it/ {
    proxy_pass http://u_it/;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_read_timeout 7200s;
    proxy_send_timeout 7200s;
  }
  location = /it { return 301 /it/; }

  # ── Timesketch (proxy sous-chemin) ────────────────────────
  # Le SPA Vue appelle /api/v1 et /sketch/* en chemins absolus (sans /timesketch/)
  location /api/v1/ {
    proxy_pass http://$ts_upstream;
    proxy_http_version 1.1;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Prefix /timesketch;
    proxy_read_timeout 3600s;
  }
  location /sketch/ {
    proxy_pass http://$ts_upstream;
    proxy_http_version 1.1;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Prefix /timesketch;
    proxy_read_timeout 3600s;
  }
  location /logout/ {
    proxy_pass http://$ts_upstream;
    proxy_http_version 1.1;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_read_timeout 120s;
  }
  # Timesketch — login POST en chemin absolu /login/ (hors /timesketch/)
  location /login/ {
    proxy_pass http://$ts_upstream;
    proxy_http_version 1.1;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Prefix /timesketch;
    # Les redirections TS absolues (/login.html legacy, /login, /) doivent rester
    # dans l'espace Timesketch — sinon /login.html tombe sur le portail CERT (P03).
    proxy_redirect /login.html https://$host/timesketch/login/;
    proxy_redirect ~^/login/?$ https://$host/timesketch/login/;
    proxy_redirect /sketch/ https://$host/sketch/;
    # Ne JAMAIS réécrire un Location déjà préfixé /timesketch/ (TS honore
    # X-Forwarded-Prefix) — sinon double préfixe /timesketch/timesketch/login/
    # → ERR_TOO_MANY_REDIRECTS (audit V04).
    proxy_redirect ~^/(?!timesketch(?:/|$)) https://$host/timesketch/;
    proxy_read_timeout 120s;
  }
  location = /login {
    return 301 /login/;
  }
  # La console référence /dist/* en chemins absolus — proxy dédié
  location /dist/ {
    proxy_pass http://$ts_upstream;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_read_timeout 3600s;
  }
  location /timesketch/ {
    rewrite ^/timesketch/(.*)$ /$1 break;
    proxy_pass http://$ts_upstream;
    proxy_http_version 1.1;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Prefix /timesketch;
    proxy_read_timeout 3600s;
    # Même garde-fou que le bloc /login/ : les redirections TS absolues ne doivent
    # jamais sortir de l'espace /timesketch/ (P03 — /login.html tombait sur le CERT).
    proxy_redirect /login.html https://$host/timesketch/login/;
    proxy_redirect ~^/login/?$ https://$host/timesketch/login/;
    proxy_redirect /sketch/ https://$host/timesketch/sketch/;
    # Ne JAMAIS réécrire un Location déjà préfixé /timesketch/ (TS honore
    # X-Forwarded-Prefix) — sinon double préfixe /timesketch/timesketch/login/
    # → ERR_TOO_MANY_REDIRECTS (audit V04).
    proxy_redirect ~^/(?!timesketch(?:/|$)) https://$host/timesketch/;
    proxy_set_header Accept-Encoding "";
    sub_filter_once off;
    # text/html est déjà inclus par défaut — ne pas le redéclarer (warn duplicate MIME)
    sub_filter_types application/javascript text/javascript;
    sub_filter '<head>' '<head><base href="/timesketch/">';
    sub_filter 'src="/dist/' 'src="/timesketch/dist/';
    sub_filter 'href="/dist/' 'href="/timesketch/dist/';
    sub_filter 'baseURL:"/api/v1"' 'baseURL:"/timesketch/api/v1"';
    sub_filter "baseURL:'/api/v1'" "baseURL:'/timesketch/api/v1'";
    sub_filter '"/login.html"' '"/timesketch/login/"';
    sub_filter "'/login.html'" "'/timesketch/login/'";
    sub_filter 'href="/login"' 'href="/timesketch/login"';
  }
  location /timesketch/api/v1/ {
    rewrite ^/timesketch/api/v1/(.*)$ /api/v1/$1 break;
    proxy_pass http://$ts_upstream;
    proxy_http_version 1.1;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Prefix /timesketch;
    proxy_read_timeout 3600s;
  }
  location = /timesketch { return 301 /timesketch/; }

  # ── OpenSearch Dashboards (SERVER_BASEPATH=/dashboards) ───
  location /dashboards {
    proxy_pass http://$osd_upstream;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
  }
  location = /dashboards { return 301 /dashboards/; }

  # ── Grafana (sous-chemin /grafana/) ───────────────────────
  location /grafana/ {
    if ($request_method = OPTIONS) {
      add_header Access-Control-Allow-Origin $grafana_cors_origin always;
      add_header Access-Control-Allow-Credentials "true" always;
      add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
      add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-Grafana-Org-Id, X-Requested-With" always;
      return 204;
    }
    include /etc/nginx/snippets/grafana-proxy.conf;
    proxy_pass http://$grafana_upstream;
  }
  location /grafana {
    if ($request_method = OPTIONS) {
      add_header Access-Control-Allow-Origin $grafana_cors_origin always;
      add_header Access-Control-Allow-Credentials "true" always;
      return 204;
    }
    include /etc/nginx/snippets/grafana-proxy.conf;
    proxy_pass http://$grafana_upstream;
  }
  location = /grafana { return 301 /grafana/; }

  # ── OpenCTI ───────────────────────────────────────────────
  location /cti/ {
    proxy_pass http://$opencti_upstream;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header X-Forwarded-Host $http_host;
    proxy_set_header X-Forwarded-Port $fp_forwarded_port;
    proxy_connect_timeout 180s;
    proxy_read_timeout 900s;
    proxy_send_timeout 900s;
    proxy_next_upstream error timeout http_502 http_503 http_504;
    proxy_next_upstream_tries 3;
    proxy_next_upstream_timeout 120s;
  }
  location = /cti { return 301 /cti/; }

  # ── TheHive ───────────────────────────────────────────────
  location /thehive/ {
    proxy_pass http://$thehive_upstream;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header X-Forwarded-Host $http_host;
    proxy_set_header X-Forwarded-Port $fp_forwarded_port;
    proxy_read_timeout 300s;
  }
  location = /thehive { return 301 /thehive/; }

  # CERT shell CSS — priorité sur le proxy MISP des chemins absolus /css/*
  location = /css/cert-shell.css {
    proxy_pass http://u_cert;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  # GET /users/login → /misp/users/login (login AJAX MISP derrière sous-chemin)
  location = /users/login {
    if ($request_method = GET) {
      return 302 /misp/users/login$is_args$args;
    }
    include /etc/nginx/snippets/misp-root-paths.conf;
    proxy_pass http://$misp_upstream;
  }

  # ── MISP chemins absolus (login POST /users/login sans préfixe) ──
  location ~ ^/(users|events|attributes|servers|organisations|admin|img|css|js|fonts|favicon\.ico)(/|$) {
    include /etc/nginx/snippets/misp-root-paths.conf;
    proxy_pass http://$misp_upstream$request_uri;
  }

  # MISP assets sous /misp/ — sans sub_filter (sinon CSS/JS → 302 login)
  location ^~ /misp/css/ {
    rewrite ^/misp/css/(.*)$ /css/$1 break;
    include /etc/nginx/snippets/misp-root-paths.conf;
    proxy_pass http://$misp_upstream;
  }
  location ^~ /misp/js/ {
    rewrite ^/misp/js/(.*)$ /js/$1 break;
    include /etc/nginx/snippets/misp-root-paths.conf;
    proxy_pass http://$misp_upstream;
  }
  location ^~ /misp/img/ {
    rewrite ^/misp/img/(.*)$ /img/$1 break;
    include /etc/nginx/snippets/misp-root-paths.conf;
    proxy_pass http://$misp_upstream;
  }
  location ^~ /misp/fonts/ {
    rewrite ^/misp/fonts/(.*)$ /fonts/$1 break;
    include /etc/nginx/snippets/misp-root-paths.conf;
    proxy_pass http://$misp_upstream;
  }

  # ── MISP (sous-chemin ; MISP_PUBLIC_BASE_URL=https://$host/misp/) ──
  # rewrite : MISP écoute en racine (/users/login) — le préfixe /misp/ est
  # porté par MISP.baseurl + App.base pour la génération d'URL côté app.
  # Sans rewrite, REQUEST_URI garde /misp/… et FormHelper hashe /misp/misp/…
  # (App.base doublé) → CSRF 400 au login.
  # Anti double-préfixe CakePHP (/misp/misp/ → /misp/)
  location = /misp/misp { return 301 /misp/; }
  location /misp/misp/ { rewrite ^/misp/misp/?(.*)$ /misp/$1 permanent; }

  location /misp/ {
    rewrite ^/misp/?(.*)$ /$1 break;
    include /etc/nginx/snippets/misp-root-paths.conf;
    proxy_pass http://$misp_upstream;
  }
  location = /misp { return 301 /misp/; }

  # ── MinIO Console (sous-chemin ; MINIO_BROWSER_REDIRECT_URL=/minio/) ──
  location /minio/ {
    rewrite ^/minio/(.*)$ /$1 break;
    proxy_pass http://$minio_console_upstream;
    proxy_http_version 1.1;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header Host               $host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Prefix /minio;
    proxy_redirect off;
    proxy_read_timeout 300s;
    sub_filter_once off;
    sub_filter_types application/javascript;
    sub_filter '<base href="/"/>' '<base href="/minio/">';
  }
  location = /minio { return 301 /minio/; }

  # ── Cortex ──────────────────────────────────────────────
  location /cortex/ {
    rewrite ^/cortex/(.*)$ /$1 break;
    proxy_pass http://$cortex_upstream;
    proxy_redirect https://localhost/ https://$http_host/;
    proxy_redirect http://localhost/ http://$http_host/;
    proxy_redirect ~^https?://[^/]+/cortex/(.*)$ https://$http_host/cortex/$1;
    proxy_redirect ~^https?://[^/]+/(.*)$ https://$http_host/cortex/$1;
    proxy_redirect /index.html https://$http_host/cortex/index.html;
    proxy_redirect ~^/(.*)$ https://$http_host/cortex/$1;
    proxy_cookie_path / /cortex/;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_set_header Host               $http_host;
    proxy_set_header X-Forwarded-Host   $http_host;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header X-Forwarded-Port   $fp_forwarded_port;
    proxy_set_header X-Real-IP          $remote_addr;
    proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
    proxy_read_timeout 300s;
  }
  location = /cortex { return 301 /cortex/; }

  # ── HELK sidecar (Kibana + Elasticsearch API) ────────────────
  # Même motif qu'OpenSearch Dashboards (/dashboards) — pas de double basePath.
  location /helk/kibana {
    proxy_pass http://$helk_kibana_upstream;
    proxy_redirect off;
    proxy_set_header Host $http_host;
    proxy_set_header X-Forwarded-Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
  }
  location = /helk/kibana { return 301 /helk/kibana/; }
  location = /helk { return 301 /helk/kibana/; }

  location /helk/ {
    return 301 /helk/kibana/;
  }

  location /helk/api/ {
    rewrite ^/helk/api/(.*)$ /$1 break;
    proxy_pass http://$helk_es_upstream;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }
  location = /helk/api { return 301 /helk/api/; }

  # ── Velociraptor bridge (AVANT proxy GUI /velociraptor/) ──
  location = /velociraptor/api/health {
    proxy_pass http://$vr_bridge_upstream/health;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }
  location = /velociraptor/api/clients {
    proxy_pass http://$vr_bridge_upstream/clients;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }
  location /velociraptor/api/export/ {
    rewrite ^/velociraptor/api/export/(.*)$ /export/$1 break;
    proxy_pass http://$vr_bridge_upstream;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_read_timeout 3600s;
  }
  location /velociraptor/api/lab/ {
    rewrite ^/velociraptor/api/lab/(.*)$ /lab/$1 break;
    proxy_pass http://$vr_bridge_upstream;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_read_timeout 3600s;
  }
  location = /velociraptor/api/collect {
    proxy_pass http://$vr_bridge_upstream/collect;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_read_timeout 3600s;
  }
  location = /velociraptor/api { return 301 /velociraptor/api/health; }

  # ── Velociraptor GUI (base_path /velociraptor, TLS offload nginx — modèle Velocidex) ──
  # IMPORTANT: proxy_pass http:// (jamais https://) — VR écoute en plain HTTP (use_plain_http: true)
  # upstream Docker DNS (pas d'IP hôte/publique codée en dur)
  # ATTENTION: proxy_pass avec $variable + URI (/velociraptor/) n'applique PAS le remplacement
  # de préfixe → toutes les requêtes arrivaient en /velociraptor/ → 307 boucle app/index.html.
  # rewrite + proxy_pass sans URI corrige le path (VR reçoit /velociraptor/app/…).
  location /velociraptor/ {
    include /etc/nginx/snippets/velociraptor-proxy.conf;
    rewrite ^/velociraptor/(.*)$ /velociraptor/$1 break;
    proxy_pass http://$velociraptor_upstream;
    proxy_redirect off;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port $server_port;
    proxy_set_header X-Forwarded-Prefix /velociraptor;
    proxy_cookie_path / /velociraptor/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
    proxy_read_timeout 3600s;
    proxy_request_buffering off;
    proxy_buffering off;
  }
  location = /velociraptor { return 301 /velociraptor/; }

  location /opensearch/ {
    return 301 /dashboards/;
  }
  location = /opensearch { return 301 /opensearch/; }

  # ── Logstash monitoring API (http.port 9700 — pas de GUI web) ──
  location /logstash/ {
    rewrite ^/logstash/?(.*)$ /$1 break;
    proxy_pass http://$logstash_upstream;
    proxy_set_header Host               $host;
    proxy_set_header X-Real-IP          $remote_addr;
    proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_read_timeout 120s;
  }
  location = /logstash { return 301 /logstash/; }

  # ── Documentation portail (statique direct — indépendant du middleware auth cert-portal)
  # Racine /docs/ → index FR (évite 403 directory listing sans index.html)
  location = /docs/ {
    return 302 /docs/fr/platform-overview.html;
  }
  location = /docs {
    return 302 /docs/fr/platform-overview.html;
  }
  location /docs/ {
    alias /usr/share/nginx/portal-docs/;
    types {
      text/html                             html htm;
      application/json                      json;
      application/javascript                js;
      text/css                              css;
    }
    default_type text/html;
    add_header Cache-Control "no-store";
  }

  # Alias historique /cert/ → portail racine
  location = /cert { return 301 /; }
  location = /cert/ { return 301 /; }

  # ── Portail CERT (racine) ─────────────────────────────────
  location / {
    proxy_pass http://u_cert;
    proxy_set_header Host               $host;
    proxy_set_header X-Real-IP          $remote_addr;
    proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto  https;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $conn_upgrade;
  }
}
