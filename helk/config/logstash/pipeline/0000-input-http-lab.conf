# SAFE: seul point d'entrée — simulateur lab HTTP (port 8080 / hôte 18080)
input {
  http {
    port => 8080
    codec => json
    additional_codecs => { "application/json" => "json" }
    tags => ["helk-lab-http"]
  }
}
